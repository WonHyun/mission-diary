enum BoxName {
  settings,
}

enum MainNavigatorType {
  home,
  feed,
  activity,
  profile,
}

enum UserSetting {
  language,
  themeMode,
  isAllowNotification,
  isAllowNotificationRing,
}

enum MissionType {
  drink,
  exercise,
  reading,
  meal,
  wakeUp,
  sleep,
  coding,
  etc,
}

enum MissionFrequency {
  once,
  daily,
  weekly,
  monthly,
}

enum UserRank {
  bronze,
  silver,
  gold,
  platinum,
  diamond,
}

enum MediaPathType {
  file,
  url,
}

enum MediaType {
  image,
  video,
}
