enum BoxName {
  settings,
}

enum MainNavigatorType {
  home,
  feed,
  activity,
  profile,
}

enum UserSetting {
  language,
  themeMode,
  isAllowNotification,
  isAllowNotificationRing,
}

enum MissionType {
  drink,
  exercise,
  reading,
  meal,
  wakeUp,
  sleep,
  coding,
  etc,
}

enum MissionFrequency {
  once,
  daily,
  weekly,
  monthly,
}
